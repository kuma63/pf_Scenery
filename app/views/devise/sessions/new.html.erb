<div class="container">
  <h2>ログイン</h2>

  <%= form_with model: @user, url: user_session_path do |f| %>
    <div class="field row">
      <p>メールアドレス</p>
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="field row">
      <p>パスワード</p>
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>

    <div class="actions">
      <%= f.submit "ログイン" %>
    </div>
  <% end %>

  <p>会員登録がお無味でない方は<%= link_to 'こちら', new_user_registration_path %>から新規登録を行ってください</p>
</div>
